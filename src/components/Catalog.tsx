import React from 'react';

import {CatalogProps} from '../lib/interfaces';
// import {catalogText} from '../constants/index';

function Catalog({lang}: CatalogProps) {
  // const catalogTextData = catalogText[lang];

  return (
    <div>Catalog</div>
  );
}

export default Catalog;